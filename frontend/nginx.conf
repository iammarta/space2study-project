server {
    listen 80;
    location / {
        root /usr/share/nginx/html;
        index index.html;
        # Support React/Vue router by redirecting all requests to index.html
        try_files $uri $uri/ /index.html;
    }
}